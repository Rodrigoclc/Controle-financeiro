// agrupar letra iguais em uma variavel
    // compara cada letra
// verificar qual vareavel é maior
var nome = `banana`

for (var i in nome) {
    
    var letra = 'a'
    if (nome[i] == letra) {
        letra += nome[i]
    } else {

    }
    
   
}
console.log(letra)